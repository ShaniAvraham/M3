<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demo.appdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".PlaylistActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MyPlaylistsActivity" />
        <activity android:name=".CreatePlaylistActivity" />
        <activity android:name=".EmptyPlaylistActivity" />
        <activity android:name=".UsernameActivity" />
        <activity android:name=".CommunityActivity" />
        <activity android:name=".RequestsActivity" />
        <activity android:name=".RequestDetailsActivity" />
        <activity android:name=".NewRequestActivity" />
        <activity android:name=".CreateRequestActivity"></activity>
    </application>

</manifest>